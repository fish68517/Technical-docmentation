好的，非常棒的决定！刻意练习是掌握任何技能的唯一途径。

这里为你设计了一系列题目，难度循序渐进，涵盖了不同领域。请记住，**完成这些题目的目标不是写出完美的代码，而是熟练运用你刚刚学到的“四步拆解法”**。

对于每一道题，请你都尝试完成以下四个步骤的思考和文档记录：

1.  **定义输入与输出 (Input & Output)**
2.  **识别核心动词与名词 (Tasks & Objects)**
3.  **编写伪代码或画流程图 (Logic Flow)**
4.  **转化成可搜索的技术问题 (Searchable Questions)**

---

### 第一阶段：入门级 - 文件与数据处理

这个阶段主要练习对本地文件和简单数据结构的操作，逻辑清晰，不涉及复杂的网络或算法。

*   **题目 1：批量文件重命名器**
      *   **需求描述**：写一个脚本，扫描一个指定的文件夹（例如 `/Downloads`），找到所有 `.jpg` 格式的图片文件，并按照“拍摄日期_编号.jpg”的格式对它们进行重命名。例如，`IMG_5021.JPG` 可能会被重命名为 `2023-10-26_001.jpg`。
      *   **提示**：你需要考虑如何读取文件的元数据（EXIF 信息）来获取拍摄日期。

*   **题目 2：个人账单分类器**
      *   **需求描述**：你有一个 CSV 文件 (`transactions.csv`)，里面记录了你一个月的消费，包含三列：`日期`, `商家`, `金额`。写一个脚本读取这个文件，并根据预设的规则（例如：包含“超市”的消费算作“生活购物”，包含“地铁”的算作“交通”），为每一笔消费打上分类标签，并生成一个新的 CSV 文件 `categorized_transactions.csv`，新文件多一列“分类”。
      *   **提示**：核心是如何定义和匹配规则。

*   **题目 3：Markdown 文档链接检查器**
      *   **需求描述**：写一个脚本，检查一个指定的 Markdown (`.md`) 文件中所有的超链接（形如 `[文本](链接地址)`) 是否有效。对于本地文件链接（如 `../images/pic.png`），检查文件是否存在；对于网络链接（如 `http://example.com`），检查是否能访问通（HTTP 状态码为 200）。最后，在控制台打印出所有失效的链接和原因。

---

### 第二阶段：进阶级 - API 与网络交互

这个阶段引入了与外部世界的交互，需要处理网络请求、API 数据解析等。

*   **题目 4：实时天气预报查询工具**
      *   **需求描述**：写一个命令行工具。当用户输入一个城市名（如 `Beijing`）后，程序能调用一个免费的天气 API（例如 OpenWeatherMap API），获取该城市当前的天气（如温度、湿度、天气状况），并格式化输出到终端。
      *   **提示**：你需要先去注册一个 API key，并学习如何发送 HTTP GET 请求和解析 JSON 格式的返回数据。

*   **题目 5：GitHub 热门项目监控器**
      *   **需求描述**：写一个脚本，使用 GitHub API 获取今天创建的、Python 语言的、星标数 (stars) 排名前 10 的开源项目。将这些项目的名称、作者、星标数和项目链接，整理并保存到一个以当天日期命名的文本文件（如 `2025-09-30_github_report.txt`）中。

*   **题目 6：网站更新通知器**
      *   **需求描述**：写一个脚本，监控一个你常看的博客或新闻网站的首页。脚本需要记录下首页内容的某个标识（例如内容的哈希值或特定区域的文本）。当再次运行时，如果发现内容标识发生了变化，就通过一个服务（例如 Server酱 或 Bark）给自己发送一条手机推送通知，提醒网站已更新。

---

### 第三阶段：综合应用级 - 结合多种技术

这个阶段的题目更贴近真实世界的复杂应用，需要组合前两个阶段的多种技能。

*   **题目 7：股票数据可视化报告生成器**
      *   **需求描述**：写一个脚本，接收一个股票代码（如 `AAPL`）和时间范围（如 `2022-01-01` 到 `2023-01-01`）作为输入。从一个金融数据源（如 `yfinance` 库）获取该股票的历史数据，然后使用数据可视化库（如 Matplotlib 或 Plotly）生成一张包含收盘价走势和交易量柱状图的图片，并保存为 `AAPL_report.png`。

*   **题目 8：自动会议纪要整理器**
      *   **需求描述**：假设你有一个存放会议录音的文件夹（都是 `.mp3` 文件）。写一个脚本，遍历这个文件夹，使用一个语音转文字的 API 或库（例如 OpenAI Whisper 的 API），将每个录音文件转换成文字稿 (`.txt` 文件)。然后，进一步使用一个大语言模型 API (如 GPT) 对文字稿进行总结，提炼出会议的要点、待办事项 (Action Items)，并将总结内容附加到文字稿文件的末尾。

*   **题目 9：你的“雷达遮蔽角”问题的变种**
      *   **需求描述**：在原有脚本的基础上，增加一个 Web 界面。用户可以在网页的地图上点击一个点作为雷达位置，通过输入框设置雷达高度，然后点击“开始分析”按钮。后端接收到请求后，执行与之前脚本相同的计算，但最终不是生成 CSV，而是在网页上动态地绘制出一个极坐标图（雷达图），直观地展示在各个方位角上的最大遮蔽角。
      *   **提示**：这需要你拆解成前端和后端两个部分。后端可以用 Flask 或 Django 来接收请求和处理数据，前端需要用 JavaScript 和一个图表库（如 Chart.js 或 D3.js）来展示结果。

---

**给你的练习建议**：

*   **不要贪多求快**：每周专注攻克一到两个题目即可。
*   **文档优先**：强迫自己在写代码前，必须先完成那四步的文档。你可以用一个简单的文本文件或者 Markdown 来记录你的思考过程。
*   **分享与求助**：当你完成一个题目的拆解后，可以把你的思路（特别是伪代码和技术问题列表）分享到技术社区或给朋友看，让他们帮你审视逻辑是否清晰。
*   **不求完美**：初版实现功能即可，不要过分纠结于代码的优雅和性能。练习的重点是**思考过程**。

祝你练习愉快！坚持下去，你会发现自己看待和解决问题的能力将截然不同。